/* Ordinary Enum */
enum colours{Red, Black, Yellow};

/* Enum with assigned integral value */
enum week{Mon=99, Tue, Wed, Thur, Fri, Sat, Sun};

/* Enum without name */
enum {item1, item2};

/* Inline function */
inline add(int a, int b);

/* Function with storage class specifier */
extern foo(float* f);